name: Testsigma CI

on:
 push:
   branches:
     - main
     - master
 workflow_dispatch:

jobs:
 run-testsigma:
   runs-on: ubuntu-latest

   steps:
     - name: Checkout code
       uses: actions/checkout@v3

     - name: Run Testsigma Test Plan
       shell: pwsh
       run: ./testsigma-trigger.ps1
