name: .NET Test Automation

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: '9.0.201' # Updated to your SDK version

    - name: Change directory to project folder
      run: cd Pranjal_Commons_Git_Actions

    - name: Restore dependencies
      run: dotnet restore Commons_Automation.sln

    - name: Build the project
      run: dotnet build --configuration Release --no-restore --property:Platform="Any CPU" Commons_Automation.sln

    - name: Run tests
      run: dotnet test --no-restore --verbosity normal Commons_Automation.sln
